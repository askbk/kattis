#include <iostream>
#include <map>
#include <string>

using namespace std;

int main() {
  string input;
  map<string, int> map;
  while (scanf("%s", &input) != "***") {
    candidates[input] += 1;
  }
}
